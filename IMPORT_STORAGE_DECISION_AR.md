# قرار مساحة استيراد قاعدة الأرقام

## المقارنة

المشروع السابق يحذف `contactsdb.db` أولًا، ثم ينسخ الملف المختار مباشرة إلى نفس الاسم النهائي داخل مجلد قواعد التطبيق. لذلك لا توجد نسخة `.importing` ولا `.backup`، لكنه يفقد القاعدة القديمة فورًا إذا توقف النسخ أو انقطع التخزين قبل اكتماله.

المشروع الجديد ينسخ إلى `contactsdb.db.importing`، يتحقق من المخطط ويجهز الفهارس، ثم ينقل القاعدة القديمة إلى `.backup` ويعيد تسمية الملف المؤقت إلى الاسم النهائي. لهذا تظهر نسختان أو ثلاث نسخ مؤقتًا داخل مساحة التطبيق. هذا السلوك مقصود لحماية القاعدة القديمة، لكنه غير مناسب إذا كانت مساحة الهاتف المتاحة أقل من حجم القاعدة الجديدة زائد حجم القاعدة الحالية.

## القرار التقني

لا أوصي بالعودة إلى طريقة المشروع السابق التي تحذف القاعدة قبل النسخ؛ فهي تحقق استيرادًا مباشرًا لكنها تجعل انقطاع عملية استيراد 8GB يؤدي إلى تطبيق بلا قاعدة بحث.

الخيار الآمن لتقليل المساحة هو **وضع الاستيراد الاقتصادي**: يحذف القاعدة القديمة بعد تأكيد المستخدم بأن لديه نسخة المصدر، ثم ينسخ الملف مباشرة إلى الاسم النهائي، مع احتفاظ التطبيق بعلامة `importing` لمنع البحث أثناء النسخ. هذا يقلل التخزين داخل بيانات التطبيق إلى نسخة واحدة تقريبًا، لكنه يتخلى عمدًا عن إمكانية استرجاع القاعدة السابقة داخل التطبيق.

## آثار كل خيار

| الخيار | أعلى مساحة مؤقتة داخل التطبيق | الاسترجاع بعد فشل النسخ | النتيجة |
|---|---:|---|---|
| الاستبدال الآمن الحالي | القاعدة القديمة + المؤقتة + النسخة الاحتياطية لفترة قصيرة | متاح | يحمي البيانات لكنه يحتاج مساحة كبيرة |
| طريقة المشروع السابق | القاعدة الجديدة فقط بعد حذف القديمة | غير متاح | أقل مساحة لكنه قد يترك التطبيق بلا قاعدة |
| الاستيراد الاقتصادي المقترح | القاعدة الجديدة فقط بعد حذف القديمة | غير متاح داخل التطبيق | أقل مساحة مع تحذير وتأكيد واضحين |

توجد نسخة إضافية محتملة من `file_picker` في مساحة Cache الخاصة بالتطبيق عند اختيار الملف على Android. الحزمة تدعم `clearTemporaryFiles()` على Android؛ يجب استدعاؤها بعد نجاح أو فشل الاستيراد لتنظيف هذه النسخة المؤقتة إن وُجدت، لكن هذا لا يحذف الملف الأصلي الذي اختاره المستخدم من مدير الملفات.

## التصحيح المطبق

أضيف تنظيف `FilePicker.platform.clearTemporaryFiles()` داخل `finally` في `DatabaseImportController.selectAndImport`. لذلك ينفذ التنظيف بعد انتهاء النسخ والتحقق والاستبدال، سواء نجحت العملية أو فشلت أو ألغى المستخدم الاختيار. لا ينفذ التنظيف أثناء قراءة ملف المصدر، فلا يؤثر في استيراد القاعدة أو في الملف الأصلي خارج بيانات التطبيق.
