# إصلاح اختبار الواجهة

- [x] إزالة توقع `CircularProgressIndicator` لأنه يعتمد على توقيت فتح SQLite في بيئة الاختبار.
- [x] التحقق بدلًا منه من عنوان التطبيق واتجاه RTL دون افتراض حالة SQLite الزمنية.
- [ ] إعادة تجهيز الحزمة لاختبار `flutter test` على جهاز Windows قبل أي Commit أو Push.

## مراجعة مساحة الاستيراد

- [x] مقارنة خدمة استيراد المشروع السابق مع `DatabaseImportService` الحالية خطوة بخطوة.
- [x] تحديد الملفات التي تتعايش أثناء الاستيراد وبعده: المصدر، `.importing`، الهدف، و`.backup`.
- [x] تقرير ما إذا كانت المساحة المؤقتة ضرورية، وما هو تعديل الاستيراد المباشر الآمن الممكن دون فقدان القاعدة.

## تنظيف Cache بعد الاستيراد

- [x] استدعاء `FilePicker.platform.clearTemporaryFiles()` بعد انتهاء النسخ والتحقق أو بعد فشل العملية، وليس قبل اكتمال استخدام الملف المختار.
- [ ] تحديث حزمة الإصلاح وتعليمات التحقق من أن «الذاكرة المؤقتة» لم تعد تحتوي نسخة 7GB بعد الاستيراد.

## تشخيص فشل الاستيراد الكبير

- [ ] الحصول على سجل Flutter/Logcat الذي يحتوي رسالة `فشل استيراد قاعدة البيانات` أو Stack Trace بعد محاولة الاستيراد.
- [x] منع تنظيف Cache قبل التأكد من نجاح نقل قاعدة SQLite إلى الاسم النهائي وكتابة علامة الجاهزية.
- [ ] إضافة مرحلة تقدم مستقلة بعد اختيار الملف توضح أن Android/file_picker ينسخ الملف إلى Cache قبل أن يبدأ التطبيق بالنسخ الداخلي.
- [x] عرض رسالة خطأ عربية قابلة للنسخ توضح المرحلة التي أخفقت بدل العودة إلى حالة «لم يتم استيراد» فقط.
- [x] إضافة تنظيف تلقائي ويدوي لـ Cache المتبقية من الاستيرادات السابقة دون حذف قاعدة الأرقام المثبتة.

## تثبيت قاعدة كبيرة دون تعليق

- [x] إزالة إنشاء فهرس `names` من مسار الاستيراد؛ استعلام الاسم الحالي يبدأ ببادئة ويمكن تشغيله دون بناء فهرس ضخم.
- [x] عدم تشغيل `COUNT(*)` قبل إتاحة البحث؛ استخدام تقدير سريع أو عداد مؤجل لا يحجب الواجهة.
- [x] تثبيت الملف بعد تحقق المخطط فقط، ثم كتابة علامة الجاهزية قبل أي تحسين اختياري ثقيل.
- [x] إظهار مرحلة تشرح أن ملف Android المؤقت يجهز قبل عودة الاختيار إلى التطبيق، ثم مرحلة النسخ الداخلي فقط.
