import 'package:flutter/material.dart';

abstract final class AppColors {
  static const primary = Color(0xFF0A5C66);
  static const background = Color(0xFFF7F9FA);
  static const cardBackground = Colors.white;
  static const textDark = Color(0xFF1F2933);
  static const textMuted = Color(0xFF607D8B);
  static const phone = Colors.blue;
  static const success = Colors.green;
  static const error = Colors.red;
  static const divider = Color(0xFFE4E7EB);
}
