# حزمة الإصلاح الجذري لـ `offline_caller_id`

## نقطة الأساس

هذه الحزمة مبنية على آخر نسخة مرفوعة إلى GitHub عند Commit `80ce527829ef1d70ab5786b13362dadd7d2dba40`. لا تحتوي على Commit أو Push جديدين.

## ما تم إصلاحه

| المجال | الإصلاح |
|---|---|
| RTL | يطبق التطبيق `Locale('ar')` و`Directionality.rtl` على مستوى `MaterialApp`، بما يشمل الإعدادات والأذونات وإدارة القاعدة والشركات. |
| دوران البحث بعد إعادة التشغيل | يمنع `DatabaseConnection` إنشاء قاعدة فارغة، ويفتح قاعدة SQLite المستوردة للقراءة فقط بعد التحقق من وجود ملفها وعلامة الجاهزية. |
| تجمد ما بعد الاستيراد | نُقلت الفهارس من `onOpen` إلى خطوة ظاهرة داخل الاستيراد بعنوان «جاري تجهيز فهارس البحث للمرة الأولى». |
| فشل فتح القاعدة | تعرض الشاشة رسالة عربية، مع إعادة محاولة وفتح إدارة قاعدة البيانات، بدل مؤشر دوران مستمر. |
| استبدال القاعدة | يحتفظ الاستيراد بنسخة احتياطية ويستعيدها عند فشل الاستبدال، كما ينظف آثار عملية متقطعة في التشغيل التالي. |
| خدمة المكالمات | لا تحجب بدء التطبيق، ولا تبدأ تلقائيًا. تبدأ فقط بعد تفعيل المستخدم للتعرف على المتصل ومنح إذن الهاتف. |
| النافذة العائمة | ترتبط فعليًا بمفتاح النافذة العائمة وصلاحيتها، ويضاف تعريف `OverlayService` الموثق في Android Manifest. |

## التطبيق على جهاز Windows

استخرج ملفات الحزمة فوق جذر مشروع `offline_caller_id` المرفوع حاليًا، ثم نفذ:

```bat
flutter clean
flutter pub get
flutter analyze
flutter test
flutter run
```

لا تنفذ Commit أو Push قبل اكتمال جميع مراحل الاختبار التالية.

## اختبار الهاتف الإلزامي

| الاختبار | النتيجة المقبولة |
|---|---|
| فتح التطبيق دون قاعدة مستوردة | تظهر بطاقة الاستيراد أو رسالة عربية واضحة، ولا يدور التطبيق بلا نهاية. |
| استيراد قاعدة كبيرة | يظهر النسخ ثم التحقق ثم تجهيز الفهارس، ويبقى التطبيق مستجيبًا. |
| إغلاق التطبيق وفتحه بعد الاستيراد | تظهر الواجهة ويعمل البحث بالاسم والرقم دون إعادة استيراد. |
| فتح الإعدادات والأذونات والشركات | كل العناصر RTL: العناوين، أزرار الرجوع، الأيقونات، وListTile. |
| تعطيل التعرف على المتصل | لا تعمل خدمة الخلفية بعد إعادة تشغيل التطبيق. |
| تفعيل التعرف + إذن الهاتف + النافذة العائمة | تبدأ الخدمة، وتظهر النافذة العائمة عند مكالمة واردة فقط إذا كان مفتاح النافذة مفعلًا. |
| قطع التطبيق أثناء استبدال قاعدة | عند إعادة التشغيل يستعيد آخر ملف صالح بدل البقاء في حالة دوران. |

## تحديث خاص بالقواعد الكبيرة

في الإصدار المحدث لا ينشئ التطبيق فهارس SQLite ضخمة أثناء الاستيراد ولا ينفذ `COUNT(*)` قبل فتح البحث. يمر شريط التقدم عبر: **نسخ داخلي حتى 80%، التحقق، التثبيت، ثم النجاح**. أما تحضير Android لنسخة مؤقتة داخل مدير الملفات فيسبق عودة التحكم للتطبيق؛ لذلك لا يمكن للتطبيق قياس تقدمه بدقة، لكنه يوضح ذلك قبل فتح مدير الملفات.

إذا فشل الاستيراد، انسخ النص الظاهر في رسالة الحالة؛ سيحتوي الآن على اسم المرحلة التي أخفقت، مثل «نسخ الملف إلى مساحة التطبيق» أو «التحقق من مخطط قاعدة البيانات» أو «تثبيت قاعدة البيانات». بعد تحديث التطبيق يمكن فتح **إدارة قاعدة البيانات** واستخدام زر **تنظيف الملفات المؤقتة للاستيراد** لمسح Cache القديمة دون حذف قاعدة الأرقام المثبتة.

## ملاحظة Android

يتضمن Manifest تعريف `OverlayService` المتوافق مع وثائق `flutter_overlay_window 0.5.0`. إذا ظهر تعارض Manifest قديم من ملفات يدوية في مشروعك المحلي، احذف تعريفات Overlay أو BackgroundService المكررة خارج `android/app/src/main/AndroidManifest.xml` قبل البناء، ثم نفذ `flutter clean`.
